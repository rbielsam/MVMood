[phases.setup]
nixPkgs= ["php82", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.xml", "php82Extensions.curl", "php82Extensions.zip", "composer"]

[phases.build]
cmds=['composer install --no-dev --optimize-autoloader"]

[phases.deploy]
cmd= "php artisan migrate --force && php artisan db:seed --class=AdminSeeder && php artisan serve --host=0.0.0.0 --port=$PORT"

